# 私有仓库部署与公开测试链接

完整、按当前 Streamlit Community Cloud 界面核对过的步骤见 `README_TEST_DEPLOY.md`。

关键点：

- GitHub 仓库可以保持 Private。
- Streamlit 应用必须在 **Settings → Sharing** 中设置为 **This app is public and searchable**，测试者才可以不登录直接打开。
- 百炼密钥只填写在 Streamlit **Advanced settings → Secrets**，不能提交到 GitHub。
- 当前项目需要三个 Secrets 字段：`DASHSCOPE_API_KEY`、`DASHSCOPE_WORKSPACE_ID`、`DASHSCOPE_MODEL`。
- 只有实际打开 `.streamlit.app` 链接并完成主流程后，才可将它发给测试者。

